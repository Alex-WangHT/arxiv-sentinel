# arxiv-sentinel — 严格遵循 SPEC §2 技术栈约束
# Python 3.10+

# === LLM 接口（硅基流动，OpenAI 兼容协议） ===
openai>=1.40.0

# === arXiv 数据源 ===
arxiv>=2.1.0
feedparser>=6.0.0
requests>=2.31.0

# === PDF 处理（SPEC 强制依赖） ===
PyMuPDF>=1.24.0          # 提取 Intro 文本（§4 Step 4 备用）
pdfplumber>=0.11.0       # 双栏裁剪文本提取（§4 Step 4 主路径）
pdf2image>=1.17.0        # PDF 转图片（§4 Step 5 多模态输入）
Pillow>=10.2.0           # pdf2image 依赖

# === 元数据与模板渲染 ===
PyYAML>=6.0.1            # YAML Front Matter 处理
Jinja2>=3.1.3            # paper_template.md 渲染

# === 重试与频率控制 ===
tenacity>=8.2.3          # 指数退避（§6 Rate Limiting）

# === 静态站点（GitHub Pages） ===
mkdocs>=1.5.0
mkdocs-material>=9.5.0
pymdown-extensions>=10.7

# === 测试 ===
pytest>=7.4.0
